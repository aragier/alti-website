services:
  profeli:
    build: .
    image: aragz/ganesha:latest
    container_name: profeli-website
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
